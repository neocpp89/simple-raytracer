#include "Camera.hpp"

namespace simple_raytracer {

/*
    The camera class doesn't have any complexity at the moment,
    so this file is empty (but this is not an error).
*/

} // namespace simple_raytracer
